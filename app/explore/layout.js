export const metadata = {
  title: 'LinkReads | Explore',
  description: 'Goodreads for the internet.',
  creator: 'Kevin Xu',
}

export default function ExploreLayout({ children }) {
  return <section>{children}</section>
}